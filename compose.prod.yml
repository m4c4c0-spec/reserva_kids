services:
  backend:
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      COOKIE_SECURE: "true"
      TRUST_PROXY: "true"

  db:
    ports:
      - "127.0.0.1:5432:5432"

  mailpit:
    profiles:
      - dev

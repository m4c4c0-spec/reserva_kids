# Java / Maven
backend/target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# Node
frontend/node_modules/
frontend/dist/
frontend/.nuxt/
frontend/.output/

# Entorno (secretos reales NUNCA al repo — RNF-02b)
.env
*.env.local
# Env de despliegue Cloud Run (tiene DB_PASSWORD y JWT_SECRET reales)
backend/deploy/cloudrun.env.yaml

# IDE / SO
.idea/
.vscode/
*.iml
.DS_Store

# Herramientas de IA (artefactos locales)
.aider*
.claude/
.mcp.json

# Fuentes de diseño (mockups/exports) — no son código, viven aparte
/Diseno_*.zip
/Diseno*.zip
/DISENO_*.png
